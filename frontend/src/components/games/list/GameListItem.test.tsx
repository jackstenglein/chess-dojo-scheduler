import { render } from '@/test-utils';
import { IncompleteIcon } from './GameListItem';

describe('IncompleteIcon', () => {
    test('renders', () => {
        render(<IncompleteIcon />);
    });
});
