import { render } from '@/test-utils';
import { UnpublishedGameBanner } from './UnpublishedGameBanner';

describe('UnpublishedGameBanner', () => {
    test('renders', () => {
        render(<UnpublishedGameBanner />);
    });
});
