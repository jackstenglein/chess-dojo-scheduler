import { render } from '@/test-utils';
import CalendarTutorial from './CalendarTutorial';

describe('CalendarTutorial', () => {
    test('renders', () => {
        render(<CalendarTutorial />);
    });
});
