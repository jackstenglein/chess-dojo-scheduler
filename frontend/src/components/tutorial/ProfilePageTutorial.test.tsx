import { render } from '@/test-utils';
import ProfilePageTutorial from './ProfilePageTutorial';

describe('ProfilePageTutorial', () => {
    test('renders', () => {
        render(<ProfilePageTutorial />);
    });
});
