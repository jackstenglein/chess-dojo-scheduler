import { render } from '@/test-utils';
import { HeatmapOptions } from './HeatmapOptions';

describe('HeatmapOptions', () => {
    test('renders', () => {
        render(<HeatmapOptions />);
    });
});
