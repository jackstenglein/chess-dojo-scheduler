import { render } from '@/test-utils';
import AccountStatusCard from './AccountStatusCard';

describe('AccountStatusCard', () => {
    test('renders', () => {
        render(<AccountStatusCard />);
    });
});
