import { Typography } from '@mui/material';

interface ModelGameListProps {
}

const ModelGameList: React.FC<ModelGameListProps> = ({}) => {
    return <Typography variant="h3">TODO</Typography>
}

export default ModelGameList;
